apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: traefik
  namespace: default
spec:
  chart:
    git: https://containous.github.io/traefik-helm-chart
    ref: master
    path: traefik